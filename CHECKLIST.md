# ✅ Чеклист запуска Linap2

## 🎯 Порядок действий

### 1️⃣ Установка зависимостей
```bash
cd C:\Users\ArT\pyprojects\Linap2
poetry install
```
- [ ] Зависимости установлены без ошибок

### 2️⃣ Настройка базы данных

Создайте файл `.env` в корне `Linap2\`:
```env
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ваш_пароль
POSTGRES_DB=linap2
API_BASE_PORT=8000
SECRET_KEY=super-secret-key-change-me
```
- [ ] Файл .env создан
- [ ] PostgreSQL запущен
- [ ] База данных создана

### 3️⃣ Инициализация таблиц
```bash
poetry run alembic upgrade head
```
- [ ] Миграции выполнены успешно
- [ ] Таблицы созданы в БД

### 4️⃣ Запуск сервера
```bash
poetry run uvicorn app.main:app --reload
```
- [ ] Сервер запустился без ошибок
- [ ] Видите сообщение: "Application startup complete"

### 5️⃣ Проверка работы

Откройте в браузере:
- [ ] http://localhost:8000/ - фронтенд загружается
- [ ] http://localhost:8000/docs - Swagger UI открывается
- [ ] http://localhost:8000/health - возвращает `{"status": "ok"}`

### 6️⃣ Тестирование функционала

#### Регистрация:
1. На главной странице нажмите "Войти"
2. Переключитесь на "Регистрация"
3. Заполните форму:
   - Username: `testuser`
   - Email: `test@example.com`
   - Password: `password123`
4. Нажмите "Зарегистрироваться"
- [ ] Регистрация прошла успешно
- [ ] Вы автоматически вошли в систему
- [ ] Имя пользователя отображается в шапке

#### Вход:
1. Выйдите (обновите страницу и очистите localStorage)
2. Нажмите "Войти"
3. Введите:
   - Email: `testuser` (⚠️ используйте username, не email!)
   - Password: `password123`
- [ ] Вход выполнен успешно
- [ ] Токен сохранился в localStorage

#### Просмотр видео:
- [ ] На главной странице видны карточки видео (если есть в БД)
- [ ] Фильтры по агентам работают
- [ ] Можно переключаться между "Агент" и "Сторона"

## 🐛 Если что-то не работает

### Ошибка: "relation does not exist"
```bash
# Пересоздайте таблицы
poetry run alembic downgrade base
poetry run alembic upgrade head
```

### Ошибка: "could not connect to server"
- Проверьте, что PostgreSQL запущен
- Проверьте настройки в .env
- Создайте базу вручную: `createdb linap2 -U postgres`

### Фронтенд не открывается
- Убедитесь, что папка `front/` существует
- Проверьте, что файлы `index.html`, `style.css`, `script.js` на месте

### "Invalid credentials" при входе
- ⚠️ В поле "Email" вводите **username**, а не email!
- Проверьте правильность пароля

### Видео не загружаются
- Это нормально, если в БД нет видео
- Создайте тестовое видео через Swagger UI (http://localhost:8000/docs)

## 🎉 Всё работает!

Если все пункты отмечены ✅ - проект полностью готов к работе!

## 📚 Что дальше?

1. Добавьте тестовые данные через Swagger UI
2. Изучите API в документации (http://localhost:8000/docs)
3. Посмотрите примеры в [FRONTEND_GUIDE.md](FRONTEND_GUIDE.md)
4. Запустите тесты: `poetry run pytest tests_api.py`

---

**Удачи с проектом! 🚀**
