# 🚀 Запуск проекта Linap2 с фронтендом

## Быстрый старт

### 1. Установка зависимостей

```bash
poetry install
```

### 2. Настройка базы данных

Создайте файл `.env` в корне проекта:

```env
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=linap2
API_BASE_PORT=8000
SECRET_KEY=your-secret-key-change-in-production
```

### 3. Инициализация БД

```bash
poetry run alembic upgrade head
```

### 4. Запуск сервера

```bash
poetry run uvicorn app.main:app --reload
```

Или просто:

```bash
python -m app.main
```

## ✅ Готово!

Откройте браузер:
- **Фронтенд**: http://localhost:8000/
- **API Docs**: http://localhost:8000/docs
- **Health Check**: http://localhost:8000/health

## 🎯 Функционал фронтенда

### Реализовано:
- ✅ Регистрация пользователей
- ✅ Вход в систему (JWT токены)
- ✅ Просмотр видео стратегий
- ✅ Фильтрация по агентам и стороне
- ✅ Личный кабинет с видео пользователя
- ✅ Автоматическое сохранение токена в localStorage

### Интеграция с API:
- `POST /api/v1/auth/register` - регистрация
- `POST /api/v1/auth/login` - вход
- `GET /api/v1/users/me` - получение текущего пользователя
- `GET /api/v1/videos/` - список всех видео
- `GET /api/v1/videos/user/{user_id}` - видео пользователя
- `POST /api/v1/videos/{id}/like` - лайк видео
- `POST /api/v1/videos/{id}/dislike` - дизлайк видео

## 📝 Как использовать фронтенд

### 1. Регистрация
1. Нажмите кнопку "Войти" в шапке сайта
2. Переключитесь на вкладку "Регистрация"
3. Заполните форму и нажмите "Зарегистрироваться"
4. После успешной регистрации вы автоматически войдете в систему

### 2. Вход
1. Нажмите кнопку "Войти"
2. Введите username (НЕ email!) и пароль
3. Нажмите "Войти"

### 3. Просмотр видео
- На главной странице отображаются все видео
- Используйте фильтры слева для выбора агента или стороны
- Нажмите "Сохранить изменения" для применения фильтров

### 4. Личный кабинет
- После входа нажмите на свой профиль в шапке
- Вы увидите свои опубликованные видео

## 🔧 Структура frontend файлов

```
front/
├── index.html      # Главная страница
├── style.css       # Стили
├── script.js       # JavaScript с API интеграцией
└── template.html   # (пустой файл, можно удалить)
```

## 🎨 Технологии фронтенда

- **HTML5** - структура страницы
- **CSS3** - стилизация (темная тема VALORANT)
- **Vanilla JavaScript** - логика приложения
- **Fetch API** - HTTP запросы к backend
- **localStorage** - сохранение JWT токена

## ⚠️ Важно

### Авторизация через username, не email
В форме входа используйте **username**, а не email! Это особенность реализации backend.

```javascript
// Правильно:
login("myusername", "password123")

// Неправильно:
login("user@example.com", "password123")  // Не сработает!
```

### CORS настроен
Backend уже настроен для работы с фронтендом:
- Разрешены все origins (`allow_origins=["*"]`)
- Разрешены все методы и заголовки
- Поддержка credentials

## 🐛 Решение проблем

### Ошибка "CORS policy"
- Убедитесь, что backend запущен на http://localhost:8000
- Проверьте, что CORS middleware добавлен в main.py

### Ошибка "Invalid credentials" при входе
- Проверьте, что вводите username (не email)
- Проверьте правильность пароля

### Видео не загружаются
- Откройте DevTools (F12) → Network
- Проверьте, что запросы к `/api/v1/videos/` возвращают 200 OK
- Если видео нет в БД, они не отобразятся

### Токен не сохраняется
- Проверьте localStorage в DevTools → Application → Local Storage
- Должен быть ключ `authToken` с JWT токеном

## 📊 Тестовые данные

Создайте тестового пользователя через фронтенд или API:

```bash
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "password123"
  }'
```

## 🚀 Production

Для production:

1. Измените `allow_origins` в main.py на конкретный домен
2. Установите надежный `SECRET_KEY` в .env
3. Используйте gunicorn:

```bash
gunicorn app.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

4. Настройте nginx для раздачи статики

---

**Приятной работы! 🎮**
